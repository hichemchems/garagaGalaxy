{% extends 'base.html.twig' %}

{% block title %}AvisClients{% endblock %}

{% block body %}
    <h1>AvisClients</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avis_client.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ avis_client.Nom }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ avis_client.Contenu }}</td>
            </tr>
            <tr>
                <th>Display</th>
                <td>{{ avis_client.Display ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_clients_index') }}">back to list</a>

    <a href="{{ path('app_avis_clients_edit', {'id': avis_client.id}) }}">edit</a>

    {{ include('avis_clients/_delete_form.html.twig') }}
{% endblock %}
