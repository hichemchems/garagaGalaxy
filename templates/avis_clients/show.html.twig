{% extends 'base.html.twig' %}

{% block title %}Avis{% endblock %}

{% block body %}
    <h1>Avis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avi.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ avi.name }}</td>
            </tr>
            <tr>
                <th>Surname</th>
                <td>{{ avi.surname }}</td>
            </tr>
            <tr>
                <th>Display</th>
                <td>{{ avi.display ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avis_crud_index') }}">back to list</a>

    <a href="{{ path('app_avis_crud_edit', {'id': avi.id}) }}">edit</a>

    {{ include('avis_crud/_delete_form.html.twig') }}
{% endblock %}
